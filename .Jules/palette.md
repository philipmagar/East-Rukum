## 2026-02-09 - Accessibility pass for interactive elements
**Learning:** Many interactive elements were implemented using non-semantic tags (like div for buttons) or missing basic accessibility attributes (aria-label, htmlFor). Converting these to proper semantic elements and adding labels significantly improves the UX for screen reader and keyboard users.
**Action:** Always check for semantic HTML and ARIA labels when working with icon buttons or custom interactive components.
